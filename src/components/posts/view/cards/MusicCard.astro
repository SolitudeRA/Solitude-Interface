---
import type { Post } from '@api/ghost/types';
import BaseCard from './BaseCard.astro';

interface Props {
    post: Post;
}

const { post } = Astro.props;
---

<BaseCard post={post} typeIcon="🎵">
    <div slot="decoration" class="flex items-center justify-center py-6">
        <div class="music-icon text-6xl opacity-80">🎧</div>
    </div>
</BaseCard>

<style>
    .music-icon {
        animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
        0%,
        100% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.1);
        }
    }
</style>
