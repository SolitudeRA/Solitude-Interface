---
/**
 * AboutContent - 关于页面的内容组件
 */
---

<div class="about-content">
    <section class="about-section">
        <h2 class="about-section-title">Welcome</h2>
        <p class="about-text">
            这是关于页面的内容。在这里你可以了解更多关于我和这个网站的信息。
        </p>
    </section>

    <section class="about-section">
        <h2 class="about-section-title">The Site</h2>
        <p class="about-text">
            这个网站是使用 Astro
            框架构建的，采用了现代化的设计风格，提供流畅的用户体验。
        </p>
    </section>

    <section class="about-section">
        <h2 class="about-section-title">Interests</h2>
        <div class="about-tags">
            <span class="about-tag">Web Development</span>
            <span class="about-tag">Design</span>
            <span class="about-tag">Technology</span>
            <span class="about-tag">Open Source</span>
        </div>
    </section>
</div>

<style>
    .about-content {
        display: flex;
        flex-direction: column;
        gap: 2.5rem;
        padding-top: 1rem;
    }

    .about-section {
        padding-bottom: 2rem;
        border-bottom: 1px solid var(--border);
    }

    .about-section:last-child {
        border-bottom: none;
        padding-bottom: 0;
    }

    .about-section-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--foreground);
        margin-bottom: 1rem;
        position: relative;
        padding-left: 1rem;
    }

    .about-section-title::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0.25rem;
        bottom: 0.25rem;
        width: 3px;
        background: linear-gradient(180deg, var(--chart-1) 0%, var(--primary) 100%);
        border-radius: 2px;
    }

    .about-text {
        color: var(--text-content-primary);
        font-size: 1.125rem;
        line-height: 1.75;
    }

    .about-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
    }

    .about-tag {
        display: inline-flex;
        align-items: center;
        padding: 0.5rem 1rem;
        font-size: 0.875rem;
        font-weight: 500;
        background: var(--accent);
        color: var(--accent-foreground);
        border-radius: 0.5rem;
        border: 1px solid var(--border);
        transition: all 0.2s ease;
    }

    .about-tag:hover {
        background: var(--secondary);
        border-color: var(--ring);
    }

    @media (max-width: 768px) {
        .about-section-title {
            font-size: 1.25rem;
        }

        .about-text {
            font-size: 1rem;
        }
    }

    /* ===== 深色模式样式（白色文字 + 暖色装饰） ===== */
    :global(.dark) .about-section {
        border-bottom-color: var(--article-content-table-border);
    }

    :global(.dark) .about-section-title {
        color: var(--foreground);
    }

    :global(.dark) .about-section-title::before {
        background: linear-gradient(180deg, var(--page-decorator-start) 0%, var(--page-decorator-end) 100%);
    }

    :global(.dark) .about-text {
        color: var(--text-content-primary);
    }

    :global(.dark) .about-tag {
        background: var(--article-content-code-bg);
        border-color: var(--article-content-pre-border);
        color: var(--text-content-primary);
    }

    :global(.dark) .about-tag:hover {
        background: var(--article-header-tag-bg);
        border-color: var(--article-header-tag-border);
    }

    /* 彩虹色标签效果 */
    :global(.dark) .about-tag:nth-child(4n+1) {
        border-left: 3px solid var(--page-bullet-1);
    }

    :global(.dark) .about-tag:nth-child(4n+2) {
        border-left: 3px solid var(--page-bullet-2);
    }

    :global(.dark) .about-tag:nth-child(4n+3) {
        border-left: 3px solid var(--page-bullet-3);
    }

    :global(.dark) .about-tag:nth-child(4n) {
        border-left: 3px solid var(--page-icon);
    }
</style>
